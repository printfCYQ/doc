# 1. 使用Rollup搭建开发环境

> vue2-响应式原理、模板编译原理、虚拟DOM原理、vue初渲染流程
> 
> -   1\. 使用Rollup搭建开发环境
> -   2\. Vue响应式原理，对象属性劫持
> -   3\. 实现对数组的方法劫持
> -   4\. 模板编辑原理，将模板转化成ast语法树
> -   5\. 代码生成，实现虚拟DOM
> -   6\. 通过虚拟DOM生成真实DOM

## 1\. 使用Rollup搭建开发环境

-   [rollup中文网](https://www.rollupjs.com)

```plain
npm install rollup rollup-plugin-babel @babel/core @babel/preset-env --save-dev
```
```plain
mkdir vue2-code
cd vue2-code
npm init -y
```
```json
{
  "name": "vue2-code",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "rollup -cw",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.20.7",
    "@babel/preset-env": "^7.20.2",
    "rollup": "^2.79.1",
    "rollup-plugin-babel": "^4.4.0"
  }
}
```
```javascript
// rollup可以导出一个对象，作为打包的配置文件
import babel from "rollup-plugin-babel";
export default {
  input: "./src/index.js", // 入口文件
  output: {
    file: "./dist/vue.js", // 出口文件
    name: "Vue", // global.Vue
    format: "umd", // esm es模块、commonjs模块、umd(commonjs、amd)
    sourcemap: true, // 是否可以调试代码，debugger
  },
  plugins: [
    babel({
      exclude: "node_modules/**", // 排除node_modeules下的所有文件
    }),
  ],
};
```
```javascript
{
    "presets": [
        "@babel/preset-env"
    ]
}
```

-   ​`npm run dev`打包生成`dist`目录-->`/dist/vue.js`、`/dist/vue.js.map`
-   新建`index.html`引入`vue.js`测试

```javascript
(function (global, factory) {
  typeof exports === 'object' && typeof module !== 'undefined' ? factory(exports) :
  typeof define === 'function' && define.amd ? define(['exports'], factory) :
  (global = typeof globalThis !== 'undefined' ? globalThis : global || self, factory(global.Vue = {}));
})(this, (function (exports) { 'use strict';

  var a = 100;
  var index = {
    b: 1
  };

  exports.a = a;
  exports["default"] = index;

  Object.defineProperty(exports, '__esModule', { value: true });

}));
//# sourceMappingURL=vue.js.map
```





## 2\. Vue响应式原理，对象属性劫持

### 初始化数据

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <script src="./vue.js"></script>
    <script>
      // 响应式数据，可以监控到数据的变化
      const vm = new Vue({
        // 代理数据
        data() {
          return {
            name: "cyq",
            age: 24,
          };
        },
      });
    </script>
  </body>
</html>
```
```javascript
import { initMixin } from "./init";

// 将所有的方法耦合在一起
function Vue(options) {
  // options 用户选项
  this._init(options);
}

initMixin(Vue); // 扩展了init方法

export default Vue;
```
```javascript
import { initState } from "./state";

// Vue 的 init() 方法
export function initMixin(Vue){
    // 初始化
    Vue.prototype._init = function (options){
        // options 用户配置
        // $data、$attr、$nextTick ......
        const vm = this;
        vm.$options = options // 将用户选项挂载到实例上

        // 初始化状态
        initState(vm)
    }
}
```
```javascript
export function initState(vm) {
  const ops = vm.$options; // 获取所有用户选项
  if (ops.data) {
    initDate(vm);
  }
}

function initDate(vm) {
  let data = vm.$options.data; // data 可能是 函数 或 对象
  data = typeof data === "function" ? data.call(vm) : data;
  console.log(data);
}
```



### 对象-数据劫持

-   [https://github.com/cyq-source-code/cyq-source-code-vue2/commit/f8b6c3f8306974680a142f5b829d37a998f6b225](https://github.com/cyq-source-code/cyq-source-code-vue2/commit/f8b6c3f8306974680a142f5b829d37a998f6b225)
-   index.js
-   init.js

```javascript
import { observe } from "./observe/index.js";

export function initState(vm) {
  const ops = vm.$options; // 获取所有用户选项
  if (ops.data) {
    initDate(vm);
  }
}

function proxy(vm, target, key) {
  Object.defineProperty(vm, key, {
    get() {
      return vm[target][key];
    },
    set(newValue) {
      vm[target][key] = newValue;
    },
  });
}

function initDate(vm) {
  let data = vm.$options.data; // data 可能是 函数 或 对象
  data = typeof data === "function" ? data.call(vm) : data;
  
  // 数据劫持 Object.defineProperty
  observe(data);

  vm._data = data; // 将返回的对象放到 _data 上

  // 将 vm._data.XXX 代码为 vm.XXX
  for (let key in data) {
    proxy(vm, "_data", key);
  }
}
```
```javascript
class Observer {
  constructor(data) {
    // Object.defineProperty只能劫持已经存在的属性（vue2为了解决这 单独写了一些API $set、$delete......）
    this.walk(data);
  }
  // 循环对象 对属性依次劫持
  walk(data) {
    // "重新定义" 属性---性能差
    Object.keys(data).forEach((key) => defineReactive(data, key, data[key]));
  }
}

// 属性劫持（闭包应用）
export function defineReactive(target, key, value) {
  // 如果 value 还是对象 递归。
  observe(value);

  Object.defineProperty(target, key, {
    // 取值的时候会触发 get
    get() {
      return value;
    },
    // 修改的时候会触发 set
    set(newValue) {
      if (newValue === value) return;
      observe(newValue) // 新值可能是 对象
      value = newValue;
    },
  });
}

export function observe(data) {
  // 对 data 对象 进行 数据劫持

  if (typeof data !== "object" || data == null) {
    return; // 判断是不是对象
  }

  //如果一个对象被劫持过了，就不需要再劫持了（要判断一个对象是否被劫持过，可以添加一个实例，用实例判断是否劫持过）
  return new Observer(data);
}
```
```javascript
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <script src="./vue.js"></script>
    <script>
      // 响应式数据，可以监控到数据的变化
      const vm = new Vue({
        // 代理数据
        data() {
          return {
            name: "cyq",
            age: 24,
            info: {
              address: "adf",
            },
          };
        },
      });
      console.log(vm);
      console.log(vm.name);
      console.log(vm.info.address);
    </script>
  </body>
</html>
```



## 3\. 实现对数组的方法劫持

-   [https://github.com/cyq-source-code/cyq-source-code-vue2/commit/41a56e9473d1f5648edd3e0fe986ae6962c02871](https://github.com/cyq-source-code/cyq-source-code-vue2/commit/41a56e9473d1f5648edd3e0fe986ae6962c02871)
-   index.js
-   init.js
-   state.js

```javascript
import { newArrayProto } from "./array";

class Observer {
  constructor(data) {
    // Object.defineProperty只能劫持已经存在的属性（vue2为了解决这 单独写了一些API $set、$delete......）

    Object.defineProperty(data, "__ob__", {
      value: this,
      enumerable: false, // 将__ob__变成不可枚举，（否则在walk()会进入死循环）
    });
    // data.__ob__ = this; // 1.通过__ob__访问方法属性、2.给数据添加了一个标识，存在__ob__证明劫持过了

    // 判断是否为数组 - 单独处理
    if (Array.isArray(data)) {
      data.__proto__ = newArrayProto; // 重写数组的方法

      this.observeArray(data); // 如果数组中放油对象，可以监听对象的变化
    } else {
      this.walk(data);
    }
  }
  // 循环对象 对属性依次劫持
  walk(data) {
    // "重新定义" 属性---性能差
    Object.keys(data).forEach((key) => defineReactive(data, key, data[key]));
  }
  observeArray(data) {
    // 劫持数组 [1, 2, { a: 2 }]
    data.forEach((item) => observe(item));
  }
}

// 属性劫持（闭包应用）
export function defineReactive(target, key, value) {
  // 如果 value 还是对象 递归。
  observe(value);

  Object.defineProperty(target, key, {
    // 取值的时候会触发 get
    get() {
      return value;
    },
    // 修改的时候会触发 set
    set(newValue) {
      if (newValue === value) return;
      observe(newValue); // 新值可能是 对象
      value = newValue;
    },
  });
}

export function observe(data) {
  // 对 data 对象 进行 数据劫持

  if (typeof data !== "object" || data == null) {
    return; // 判断是不是对象
  }
  if (data.__ob__ instanceof Observer) {
    // 这个对象被劫持过了
    return data.__ob__;
  }

  //如果一个对象被劫持过了，就不需要再劫持了（要判断一个对象是否被劫持过，可以添加一个实例，用实例判断是否劫持过）
  return new Observer(data);
}
```
```javascript
// 重写数组的部分方法

let oldArrayProto = Array.prototype;

export let newArrayProto = Object.create(oldArrayProto);

// 7种改变原数组的方法
// concat、slice不改变原数组
let methods = ["push", "pop", "shift", "unshift", "reverse", "sort", "splice"];

methods.forEach((method) => {
  // 重写数组方法
  newArrayProto[method] = function (...args) {
    console.log(method);
    const result = oldArrayProto[method].call(this, ...args); // 内部调用原来的方法、函数劫持（切片编程）

    // 需要对新增的数据，再进行劫持
    let inserted; // 新增的内容
    let ob = this.__ob__;
    switch (method) {
      case "push":
      case "unshift":
        // arr.push(1,2,3)、 arr.unshift(4,5,6)
        inserted = args;
        break;

      case "splice":
        // arr.splice(1, 2, { a: 2 });
        inserted = args.slice(2);
        break;
      default:
        break;
    }

    if (inserted) {
      // 对新增的内容再次进行观测
      ob.observeArray(inserted);
    }

    return result;
  };
});
```
```html
<script>
  // 响应式数据，可以监控到数据的变化
  const vm = new Vue({
    // 代理数据
    data() {
      return {
        name: "cyq",
        age: 24,
        info: {
          address: "adf",
        },
        arr: [1],
      };
    },
  });
  console.log(vm);
  console.log(vm.name);
  console.log(vm.info.address);
  vm.arr.push(1);
</script>
```



## 4\. 模板编辑原理，将模板转化成ast语法树

```javascript
npm install @rollup/plugin-node-resolve --save-dev
```
```javascript
// rollup可以导出一个对象，作为打包的配置文件
import babel from "rollup-plugin-babel";
import resolve from "@rollup/plugin-node-resolve";
export default {
  input: "./src/index.js", // 入口文件
  output: {
    file: "./dist/vue.js", // 出口文件
    name: "Vue", // global.Vue
    format: "umd", // esm es模块、commonjs模块、umd(commonjs、amd)
    sourcemap: true, // 是否可以调试代码，debugger
  },
  plugins: [
    babel({
      exclude: "node_modules/**", // 排除node_modeules下的所有文件
    }),
    resolve(),
  ],
};
```

### 解析模板参数

-   ​  
    

```javascript
import { compileToFunction } from "./compiler";
import { initState } from "./state";

// Vue 的 init() 方法
export function initMixin(Vue) {
  // 初始化
  Vue.prototype._init = function (options) {
    // options 用户配置
    // $data、$attr、$nextTick ......
    const vm = this;
    vm.$options = options; // 将用户选项挂载到实例上

    // 初始化状态
    initState(vm);

    if (options.el) {
      vm.$mount(options.el);
    }
  };

  Vue.prototype.$mount = function (el) {
    const vm = this;
    const ops = vm.$options;
    el = document.querySelector(el);
    // 检查用户选项 有没有 render 函数
    if (!ops.render) {
      let template;
      // 有没有 template 和 el
      if (!ops.template && el) {
        template = el.outerHTML;
      } else {
        if (el) {
          template = ops.template; // 用户选项写了 template 用 template
        }
      }
      console.log(template);

      if (template) {
        // 对模版进行编译
        const render = compileToFunction(template);
        ops.render = render;
      }
    }

    ops.render; // 最终的render方法
  };
}
```
```javascript
// 对模版进行编译处理
export function compileToFunction(template) {
  console.log(template);
  // 1.将 template 转化成 ast 语法树
  // 2.生成render方法，（render方法执行后的返回结果就是虚拟DOM）
}
```
```javascript
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <div id="app">
      <div>{{name}}</div>
      <div>{{age}}</div>
    </div>
    <script src="./vue.js"></script>
    <script>
      const vm = new Vue({
        // 代理数据
        data() {
          return {
            name: "cyq",
            age: 24,
          };
        },
        // el: "#app",
        // template: "<div>123<div>",
        // render(h) {
        //   return h("div", {});
        // },
      });
      vm.$mount("#app");
      //1.模板引擎  性能差需要正贝则匹配替换   1.0的时候没有引入虚拟D0M的改变
      //2.采用虚拟Dom，数据变化后比较虚拟DOM的差异最后更新需要更新的地方
      //3.核心就是我们需要将模板变成我们的js语法，通过js语法生成虚拟D0M

      //从一个东西变成另一个东西语法之间的转化  es6->es5
      //css压缩   我们需要先变成语法树在重新组装代码成为新的语法 将template语法转换成render函数
    </script>
  </body>
</html>
```



### 将 template 转化成 ast 语法树

-   ​[https://github.com/cyq-source-code/cyq-source-code-vue2/commit/9e53f419d6647e082e50b26545c8ba7ce2d22ddb](https://github.com/cyq-source-code/cyq-source-code-vue2/commit/9e53f419d6647e082e50b26545c8ba7ce2d22ddb)

## 5\. 代码生成，实现虚拟DOM & 通过虚拟DOM生成真实DOM

-   [https://github.com/cyq-source-code/cyq-source-code-vue2/commit/cb3d6fcf0fcbf158decad51ac18a859280b183f8](https://github.com/cyq-source-code/cyq-source-code-vue2/commit/cb3d6fcf0fcbf158decad51ac18a859280b183f8)

## 6\. 依赖收集（观察者模式）

-   [https://github.com/cyq-source-code/cyq-source-code-vue2/commit/32986ca4665bff40d3e1e8024b0f51f3fba7b087](https://github.com/cyq-source-code/cyq-source-code-vue2/commit/32986ca4665bff40d3e1e8024b0f51f3fba7b087)

## 7\. 异步更新（nextTick）

-   [https://github.com/cyq-source-code/cyq-source-code-vue2/commit/5a44fb4e653893cc7a16a5a30a06b6697b3bec4a](https://github.com/cyq-source-code/cyq-source-code-vue2/commit/5a44fb4e653893cc7a16a5a30a06b6697b3bec4a)

## 8\. mixin（发布订阅模式）

-   [https://github.com/cyq-source-code/cyq-source-code-vue2/commit/43b231b47db56bc55bd74f72169f1a25dfdc882d](https://github.com/cyq-source-code/cyq-source-code-vue2/commit/43b231b47db56bc55bd74f72169f1a25dfdc882d)

## 9\. 数组更新实现原理

-   [https://github.com/cyq-source-code/cyq-source-code-vue2/commit/c468fb8620380fd4f16a62e05d06389c41c670b9](https://github.com/cyq-source-code/cyq-source-code-vue2/commit/c468fb8620380fd4f16a62e05d06389c41c670b9)

## 10\. 计算属性（computed）

-   [https://github.com/cyq-source-code/cyq-source-code-vue2/commit/58f0a5aafac8f788706f257b571117b1e9532749](https://github.com/cyq-source-code/cyq-source-code-vue2/commit/58f0a5aafac8f788706f257b571117b1e9532749)

## 11\. 侦听器（watch）

-   [https://github.com/cyq-source-code/cyq-source-code-vue2/commit/814e1f5aafec766836f578c70af5eb3fb278ccac](https://github.com/cyq-source-code/cyq-source-code-vue2/commit/814e1f5aafec766836f578c70af5eb3fb278ccac)
