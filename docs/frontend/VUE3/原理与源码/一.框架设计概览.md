# 一.框架设计概览

## 1.权衡的艺术

## 2.框架设计的核心要素

## 3.Vue.js的设计思路

> -   1.vue的整体设计是什么样子的（可画一下简图），为什么这么设计？
> -   2.声明式UI和命令式UI分别有什么优缺点？你所知道的语言中有哪些是声明式UI的？哪些是命令式UI？
> -   3.如何理解运行时、编译时，vue框架是如何实现运行时+编译时？能否以我们的代码日常的代码为例举例如何体现运行时和编译时？
> -   4.vue 是从哪些方面控制框架的体积？可以看一下我们代码平时打包是什么产物？
> -   5.如果让你设计一个框架，你可以从vue中学到哪些精华的设计点？

# 二.响应式系统

## 4.响应式系统的作用与实现

> -   1.什么是副作用函数与响应式数据？
> -   2.响应式系统的根本实现原理是什么？副作用函数是如何与被操作的目标字段建立明确的联系的？可以画图说明
> -   3.设计一个完善的响应式系统，需要考虑哪些方面？
> -   4.计算属性与副作用函数是什么关系？
> -   5.watch 的实现原理是什么？vue 是如何解决竞态问题的？

## 5.非原始值的响应式方案

## 6.原始值的响应式方案

> -   1.如何理解 Proxy 中拦截对象操作的函数以及 Reflect 的方法中最后一个参数 "receiver" ？以下面代码为例，为什么不传递 "receiver" 参数，副作用函数不能正确地收集依赖和执行？
```javascript
const obj = {
  foo: 1,
  get bar() {
    return this.foo;
  },
  set bar(val) {
    this.foo = val;
  },
};
const proxy = new Proxy(obj, {
  get(target, key) {
    // track(target, key);
    return Reflect.get(target, key);
  },
  set(target, key, value) {
    Reflect.set(target, key, value);
    // trigger(target, key);
  },
});
effect(() => {
  console.log(proxy.bar);
});
proxy.foo = 2;
```
> -   2.如何理解 “常规对象” 和 “异质对象”？如何实现异质对象的响应式方案？可以以 Array/Set/Map 为例，分析它们的响应式方案的实现步骤，总结出设计响应式方案时需要考虑哪些内容。
> 
> -   3.在通过 Proxy 拦截对象 get 和 set 操作的基础上，vue 如何实现对深层对象的浅响应、深响应，以及浅只读、深只读？
> -   4.vue 中 ref 函数做了什么处理，为什么 setup 中需要用 .value 访问值，而模版中不需要？这些处理属于编译时还是运行时？
> 
> ​  
> 
> -   5.下面代码中页面内容是否能正确响应数据变化，为什么？
```vue
<template>
  <div @click="handleClick">
    {{ foo.bar }}
  </div>
</template>
<script>
  import { reactive } from 'vue';
  export default {
    setup() {
      const obj = reactive({
        foo: {
          bar: 1;
        },
      })
      const handleClick = () => {
        const { foo } = obj;
        foo.bar += 1;
      };
      return {
        ...obj,
        handleClick,
      };
    }
  }
</script>
```
> （选）你了解的其他语言/框架中数据的响应式方案是什么样的？

# 三.渲染器

## 7.渲染器的设计

## 8.挂载与更新

> -   1.渲染器是如何与响应式系统结合的？请简述并给出简单的代码示例。
> -   2.渲染器是如何实现跨平台的？请结合书中的代码示例给出在挂载时实现跨平台的代码实现。
> -   3.如何挂载子节点以及节点的属性？并请简单举例说明 HTML Attributes 和 DOM Properties 的区别。
> -   4.如何在虚拟节点中描述事件？如何把事件添加到 DOM 元素上？以及如何更新事件？
> -   5.对于一个元素来说，它的子节点有几种情况？更新时节点时共有几种可能？可画图示意。对于这些可能是如何处理的？可给出代码示例。

### 渲染器是如何与响应式系统结合的？请简述并给出简单的代码示例。

> 渲染器的作用是把虚拟 DOM 渲染为特定平台上的真实元素。在浏览器平 台上，渲染器会把虚拟 DOM 渲染为真实 DOM 元素。

> 首先定义了一个响应式数据 count，它是一个 ref，然后在副作用函数内调用 renderer 函数执行渲染。
> 
> 副作用函数执行完毕后，会与响应式数据建立响应联系。当我们修改count.value 的值时，副作用函数会重新执行，完成重新渲染。
> 
> 所以上面的代码运行完毕后，最终渲染到页面的内容是 &lt;h1>2&lt;/h1>。
```javascript
const count = ref(1)
effect(() => {
  renderer(`<h1>${count.value}</h1>`, document.getElementById('app'))
})
count.value++
```
```html
<div id="app"></div>

<script src="https://unpkg.com/@vue/reactivity@3.0.5/dist/reactivity.global.js"></script>
<script>

  function createRenderer(options) {

    const {
      createElement,
      insert,
      setElementText
    } = options

    function mountElement(vnode, container) {
      const el = createElement(vnode.type)
      if (typeof vnode.children === 'string') {
        setElementText(el, vnode.children)
      }
      insert(el, container)
    }

    function patch(n1, n2, container) {
      if (!n1) {
        mountElement(n2, container)
      } else {
        //
      }
    }

    function render(vnode, container) {
      if (vnode) {
        // 新 vnode 存在，将其与旧 vnode 一起传递给 patch 函数进行打补丁
        patch(container._vnode, vnode, container)
      } else {
        if (container._vnode) {
          // 旧 vnode 存在，且新 vnode 不存在，说明是卸载(unmount)操作
          // 只需要将 container 内的 DOM 清空即可
          container.innerHTML = ''
        }
      }
      // 把 vnode 存储到 container._vnode 下，即后续渲染中的旧 vnode
      container._vnode = vnode
    }

    return {
      render
    }
  }

  // const renderer = createRenderer({
  //   createElement(tag) {
  //     return document.createElement(tag)
  //   },
  //   setElementText(el, text) {
  //     el.textContent = text
  //   },
  //   insert(el, parent, anchor = null) {
  //     parent.insertBefore(el, anchor)
  //   }
  // })

  const vnode = {
    type: 'h1',
    children: 'hello'
  }

  // renderer.render(vnode, document.querySelector('#app'))

  const renderer2 = createRenderer({
    createElement(tag) {
      console.log(`创建元素 ${tag}`)
      return { tag }
    },
    setElementText(el, text) {
      console.log(`设置 ${JSON.stringify(el)} 的文本内容：${text}`)
      el.text = text
    },
    insert(el, parent, anchor = null) {
      console.log(`将 ${JSON.stringify(el)} 添加到 ${JSON.stringify(parent)} 下`)
      parent.children = el
    }
  })

  const container = { type: 'root' }
  renderer2.render(vnode, container)

</script>
```

### 渲染器是如何实现跨平台的？请结合书中的代码示例给出在挂载时实现跨平台的代码实现。

> mountElement 函数内调用了大量依赖于浏览器 的 API，例如 document.createElement、el.textContent 以 及 appendChild 等。想要设计通用渲染器，第一步要做的就是将这 些浏览器特有的 API 抽离。怎么做呢？我们可以将这些操作 DOM 的 API 作为配置项，该配置项可以作为 createRenderer 函数的参数。
> 
> -   【渲染器的设计】代码

### 如何挂载子节点以及节点的属性？并请简单举例说明 HTML Attributes 和 DOM Properties 的区别。

> -   setAttribute
> -   直接将属性设置在 DOM 对象上，即 `el[key] = vnode.props[key]`。
> -   **都存在缺陷**
```javascript
function mountElement(vnode, container) {
  const el = createElement(vnode.type)
  if (typeof vnode.children === 'string') {
    setElementText(el, vnode.children)
  } else if (Array.isArray(vnode.children)) {
    vnode.children.forEach(child => {
      patch(null, child, el)
    })
  }

  if (vnode.props) {
    for (const key in vnode.props) {
      el.setAttribute(key, vnode.props[key])
    }
  }

  insert(el, container)
}
```
```html
<div class="foo"></div>

<div aria-valuenow="75"></div>

<input value="foo" /> 
<!-- 用户将文本框的内容修改为 'bar' -->
console.log(el.value) // 'bar'
console.log(el.getAttribute('value')) // 仍然是 'foo'
console.log(el.value) // 'bar'
```
> -   HTML Attributes
> 
> -   指的就是定义在 HTML 标签上的属性，这里指的 就是 id="my-input"、type="text" 和 value="foo"。
> 
> -   DOM Properties
> 
> -   当浏览器解析 HTML 代码后，会创建一个与`HTML Attributes`相符的 DOM 元素对象，可以通过 JavaScript 代码来读取该 DOM 对象，这个 DOM 对象会包含很多属性，这些属性就是所谓的 DOM Properties
> 
> -   DOM Properties 与 HTML Attributes 的名字不总是一模一样的
> 
> -   class="foo" 对应的 DOM Properties 则是 el.className。
> 
> -   不是所有 DOM Properties 都有与之对应的 HTML Attributes
> 
> -   aria-\* 类的 HTML Attributes 就没有与之对应的 DOM Properties。
> 
> -   一个 HTML Attributes 可能关联多个 DOM Properties
> 
> -   value="foo" 与 el.value 和 el.defaultValue 都有关联。
> 
> 总结：HTML Attributes 的作用是设置与之对应的 DOM Pr operties 的初始值。
```html
<div id="app"></div>

<div data-a="3" mmm="asdf" draggable="true"></div>

<form id="aaa"></form>
<input value="aaa" form="aaa" />

<script src="https://unpkg.com/@vue/reactivity@3.0.5/dist/reactivity.global.js"></script>
<script>

function shouldSetAsProps(el, key, value) {
  if (key === 'form' && el.tagName === 'INPUT') return false
  return key in el
}

function createRenderer(options) {

  const {
    createElement,
    insert,
    setElementText,
    patchProps
  } = options

  function mountElement(vnode, container) {
    const el = createElement(vnode.type)
    if (typeof vnode.children === 'string') {
      setElementText(el, vnode.children)
    } else if (Array.isArray(vnode.children)) {
      vnode.children.forEach(child => {
        patch(null, child, el)
      })
    }

    if (vnode.props) {
      for (const key in vnode.props) {
        patchProps(el, key, null, vnode.props[key])
      }
    }

    insert(el, container)
  }

  function patch(n1, n2, container) {
    if (!n1) {
      mountElement(n2, container)
    } else {
      //
    }
  }

  function render(vnode, container) {
		if (vnode) {
      // 新 vnode 存在，将其与旧 vnode 一起传递给 patch 函数进行打补丁
      patch(container._vnode, vnode, container)
    } else {
      if (container._vnode) {
        // 旧 vnode 存在，且新 vnode 不存在，说明是卸载(unmount)操作
        // 只需要将 container 内的 DOM 清空即可
        container.innerHTML = ''
      }
    }
    // 把 vnode 存储到 container._vnode 下，即后续渲染中的旧 vnode
    container._vnode = vnode
  }
  
  return {
    render
  }
}

const renderer = createRenderer({
  createElement(tag) {
    return document.createElement(tag)
  },
  setElementText(el, text) {
    el.textContent = text
  },
  insert(el, parent, anchor = null) {
    parent.insertBefore(el, anchor)
  },
  patchProps(el, key, preValue, nextValue) {
    if (shouldSetAsProps(el, key, nextValue)) {
      const type = typeof el[key]
      if (type === 'boolean' && nextValue === '') {
        el[key] = true
      } else {
        el[key] = nextValue
      }
    } else {
      el.setAttribute(key, nextValue)
    }
  }
})

const vnode = {
  type: 'button',
  props: {
    disabled: ''
  },
  children: 'Button'
}
renderer.render(vnode, document.querySelector('#app'))

</script>
```
> -   检查每一个 vnode.props 中的属性， 看看是否存在对应的 DOM Properties，如果存在，则优先设置 DOM Properties。同时，我们对布尔类型的 DOM Properties 做了值的矫正， 即当要设置的值为空字符串时，将其矫正为布尔值 true。当然，如果vnode.props 中的属性不具有对应的 DOM Properties，则仍然使用 setAttribute 函数完成属性的设置；
> -   提取了一个 shouldSetAsProps 函数。该函数会返回一个布尔值，代表属性是否应该作为 DOM Properties 被设置。如果返回 true，则代表应该作为 DOM Properties 被设置，否则应该使用 setAttribute 函数来设置。

### 如何在虚拟节点中描述事件？如何把事件添加到 DOM 元素上？以及如何更新事件？

> -   我们可以约定，在 vnode.props 对 象中，凡是以字符串 on 开头的属性都视作事件 。
```javascript
const vnode = {
  type: 'p',
  props: {
    onClick: () => {
      alert('clicked')
    } 
  },
  children: 'text'
}
```
---
> 1.  直接使用`addEventListener`；
> 2.  伪造的事件处理 函数 invoker ；然后把真正的事件处理函数设置为 invoker.value 属性的值。这样当更新事件的时候，我们将不再需要调用 removeEventListener 函数来移除上一次绑定的事件，只需要更新 invoker.value 的值即可 ；
> 3.  将 el.\_vei 设计为一个对象，它的键是事件名称，它的 值则是对应的事件处理函数，这样就不会发生事件覆盖的现象了 ；
> 4.  当 invoker 函数执行时，在调用真正的事件处理函数之前，要先检查 invoker.value 的数据结构是否是数组，如果是数组则遍历它，并 逐个调用定义在数组中的事件处理函数
```javascript
const renderer = createRenderer({
  createElement(tag) {
    return document.createElement(tag)
  },
  setElementText(el, text) {
    el.textContent = text
  },
  insert(el, parent, anchor = null) {
    parent.insertBefore(el, anchor)
  },
  patchProps(el, key, prevValue, nextValue) {
    if (/^on/.test(key)) {
      const invokers = el._vei || (el._vei = {})
      let invoker = invokers[key]
      const name = key.slice(2).toLowerCase()
      if (nextValue) {
        if (!invoker) {
          invoker = el._vei[key] = (e) => {
            if (Array.isArray(invoker.value)) {
              invoker.value.forEach(fn => fn(e))
            } else {
              invoker.value(e)
            }
          }
          invoker.value = nextValue
          el.addEventListener(name, invoker)
        } else {
          invoker.value = nextValue
        }
      } else if (invoker) {
        el.removeEventListener(name, invoker)
      }
    } else if (key === 'class') {
      el.className = nextValue || ''
    } else if (shouldSetAsProps(el, key, nextValue)) {
      const type = typeof el[key]
      if (type === 'boolean' && nextValue === '') {
        el[key] = true
      } else {
        el[key] = nextValue
      }
    } else {
      el.setAttribute(key, nextValue)
    }
  }
})
```

### 对于一个元素来说，它的子节点有几种情况？更新时节点时共有几种可能？可画图示意。对于这些可能是如何处理的？可给出代码示例。

> -   没有子节点，此时 vnode.children 的值为 null。
> -   具有文本子节点，此时 vnode.children 的值为字符串，代表 文本的内容。
> -   其他情况，无论是单个元素子节点，还是多个子节点(可能是文 本和元素的混合)，都可以用数组来表示。
---
> **更新子节点时全部九种可能**



---
```javascript
  function patchChildren(n1, n2, container) {
    if (typeof n2.children === 'string') {
      if (Array.isArray(n1.children)) {
        n1.children.forEach((c) => unmount(c))
      }
      setElementText(container, n2.children)
    } else if (Array.isArray(n2.children)) {
      if (Array.isArray(n1.children)) {
        n1.children.forEach(c => unmount(c))
        n2.children.forEach(c => patch(null, c, container))
      } else {
        // 旧子节点要么是文本子节点，要么不存在
        // 但无论哪种情况，我们都只需要将容器清空，然后将新的一组子节点逐个挂载
        setElementText(container, '')
        n2.children.forEach(c => patch(null, c, container))
      }
    } else {
      // 代码运行到这里，说明新子节点不存在
      // 旧子节点是一组子节点，只需逐个卸载即可
      if (Array.isArray(n1.children)) {
        n1.children.forEach(c => unmount(c))
      } else if (typeof n1.children === 'string') {
        setElementText(container, '')
      }
    }
  }
```

## 9.简单diff算法

## 10.双端diff算法

## 11.快速diff算法
