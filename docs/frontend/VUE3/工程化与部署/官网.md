# 官网

> unipush2.0使用指南 [https://uniapp.dcloud.net.cn/unipush-v2.html](https://uniapp.dcloud.net.cn/unipush-v2.html)
> 
> 自助排查工具：Unipush常见问题 [https://ask.dcloud.net.cn/article/36611](https://ask.dcloud.net.cn/article/36611)

​  

# 推送流程



# 名词解释

| 名词 | 解释 |
| --- | --- |
| 通知消息 | 指定通知标题和内容后，由个推SDK自动处理在系统通知栏中展示通知栏消息，同时响铃或震动提醒用户(响铃和震动受手机系统的设置状态影响)。 |
| 透传消息 | 即自定义消息，消息体格式客户可以自己定义，如纯文本、json 串等。透传消息个推只传递数据，不做任何处理，客户端接收到透传消息后需要自己去做后续动作处理，如通知栏展示、弹框等。 |
| ClientId | 个推业务层中的对外用户标识，用于标识客户端身份，由第三方客户端获取并保存到第三方服务端，是个推 SDK 的唯一识别号，简称 CID。 |
| 在线推送 | app 在前台打开运行时，通过个推渠道下发消息。 |
| 离线推送 | app在后台、锁屏、进程关闭时，通过厂商渠道下发消息。若未集成 android 多厂商、未配置 ios 推送证书，则该机型无法使用离线推送。 |

# APP-在线推送

## 创建新app



## 关联云空间

-   没有点新建





## 配置manifest.json

-   勾选unipush2



## 新建云函数





## uni-cloud-push





## 开通unipush2

-   开发者后台 `[https://dev.dcloud.net.cn/](https://dev.dcloud.net.cn/)`

-   unipush
-   切换应用
-   填写信息
-   关联刚才创建的云空间
-   开通



## 编写代码

> App.vue

```javascript
<script>
	export default {
		onLaunch: function() {
			console.log('App Launch')
			uni.onPushMessage((res) => {
				console.log("收到推送消息：", res) //监听推送消息
			})
		},
		onShow: function() {
			console.log('App Show')
			uni.getPushClientId({
				success: (res) => {
					let push_clientid = res.cid
					console.log('客户端推送标识:',push_clientid)
				},
				fail(err) {
					console.log(err)
				}
			})
		},
		onHide: function() {
			console.log('App Hide')
		}
	}
</script>
```

> unipush 云函数 index.js

参考链接：[https://uniapp.dcloud.net.cn/uniCloud/http.html#input](https://uniapp.dcloud.net.cn/uniCloud/http.html#input)

> 以下是服务器调用云函数走post请求

```javascript
'use strict';
const uniPush = uniCloud.getPushManager({
	appId: "AppId"
})
exports.main = async (event) => {
	let obj = JSON.parse(event.body)
	const res = await uniPush.sendMessage({
		"push_clientid": obj.cids, // 设备id，支持多个以数组的形式指定多个设备，如["cid-1","cid-2"]，数组长度不大于1000
		"title": obj.title, // 标题
		"content": obj.content, // 内容
		"payload": obj.data, // 数据
		"force_notification": true, // 服务端推送 需要加这一句
		"request_id": obj.request_id //请求唯一标识号，10-32位之间；如果request_id重复，会导致消息丢失
	})
	return res;
};

```

如果是get请求：

```javascript
'use strict';
const uniPush = uniCloud.getPushManager({
	appId: "AppId"
})
exports.main = async (event) => {
	let obj = JSON.parse(event.queryStringParameters)
	const res = await uniPush.sendMessage({
		"push_clientid": obj.cids, // 设备id，支持多个以数组的形式指定多个设备，如["cid-1","cid-2"]，数组长度不大于1000
		"title": obj.title, // 标题
		"content": obj.content, // 内容
		"payload": obj.data, // 数据
		"force_notification": true, // 服务端推送 需要加这一句
		"request_id": obj.request_id //请求唯一标识号，10-32位之间；如果request_id重复，会导致消息丢失
	})
	return res;
};

```

-   参数详情：`[https://uniapp.dcloud.net.cn/uniCloud/uni-cloud-push/api.html#%E5%85%A5%E5%8F%82%E8%AF%B4%E6%98%8E](https://uniapp.dcloud.net.cn/uniCloud/uni-cloud-push/api.html#%E5%85%A5%E5%8F%82%E8%AF%B4%E6%98%8E)`

如果是自定义消息，需要自己在接收消息做处理，但是如果是推送通知，不考虑自定义，就指定"force\_notification": true代表以系统级通知做推送。

force\_notification 添加参数来源链接：

[uni-app官网](https://uniapp.dcloud.net.cn/unipush-v2.html#online)

## 上传云函数



## 自定义基座



## 运行

-   切换到云函数





## 云函数URL化

-   [https://unicloud.dcloud.net.cn/home](https://unicloud.dcloud.net.cn/home)





## api工具发送请求





## 手机设置

-   华为手机默认`横幅通知`是没勾选的。通知的时候下拉才能看见消息。勾选后会弹出消息。

 

# 自定义铃声

> -   [https://ext.dcloud.net.cn/plugin?id=7482](https://ext.dcloud.net.cn/plugin?id=7482)
> -   [https://uniapp.dcloud.net.cn/uniCloud/uni-cloud-push/api.html#](https://uniapp.dcloud.net.cn/uniCloud/uni-cloud-push/api.html#)入参说明

# 离线推送

参考 [https://docs.getui.com/getui/mobile/vendor/vendor\_open/](https://docs.getui.com/getui/mobile/vendor/vendor_open/)



## 华为

### 华为开发者后台

-   ​`[华为开发者后台](https://developer.huawei.com/consumer/cn/console#/serviceCards/)`



-   第一次让创建项目(根据提示下一步)



#### 项目设置



-   点最上面的添加应用
-   添加完成



-   添加SHA256证书指纹
-   红框是下一步需要用的

#### 推送服务





### dcloud开发者后台



### 代码



## IOS

> 获取证书.....
> 
> [https://ask.dcloud.net.cn/article/152](https://ask.dcloud.net.cn/article/152)



> ⚠️ 别忘了勾选



> ⚠️ 离线收不到消息
> 
> 参考： [https://ask.dcloud.net.cn/article/36611](https://ask.dcloud.net.cn/article/36611)

# other

## 获取不到cid

```vue
onShow: function() {
  console.log('App Show')
  let timer = setInterval(() => {
    this.getCid()
    const cid = uni.getStorageSync('cid')
    if (cid) clearInterval(timer);
  }, 1000)
},
  methods: {
    getCid() {
      uni.getPushClientId({
        success: (res) => {
          console.log('客户端推送标识:', res.cid)
          uni.setStorageSync('cid', res.cid)
          //do something
        },
        fail(err) {
          console.log(err)
        }
      })
    }
  },
```
