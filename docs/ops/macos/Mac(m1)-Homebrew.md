1.  新建终端

输入

```plain
/bin/bash -c "$(curl -fsSL https://cdn.jsdelivr.net/gh/ineo6/homebrew-install/install.sh)"
```

回车，安装过程提示输入密码。输入密码回车等待安装完成

​  

2.  环境变量

-   安装完成时，会提示添加环境变量，并有两行代码
-   复制代码，在终端输入并回车。等待完成

3.  验证

```plain
 brew -v
```
